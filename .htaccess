Options -Indexes
DirectoryIndex index.html

# Desativa rewrite do WordPress neste diretório
RewriteEngine Off

# Sem cache
<IfModule mod_headers.c>
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "0"
</IfModule>
